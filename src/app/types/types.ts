import { CartItem, Product } from '../interfaces/interfaces';

export type ToastType = 'success' | 'danger' | 'info';

export type CartItemArray = Array<CartItem>;
export type ProductArray = Array<Product>;
